"""Autogenerated ML Agent wrapper for HypertensionAgent.

This file was generated by the CSV Builder Script from synthetic_clinical_dataset.csv.
"""

from pathlib import Path
from typing import Any

from services.ml_mcp_server.models._agent_factory import AgentConfig, DynamicMLAgent

# --- 1. Paths & Configuration ---
_ARTIFACT_PATH = Path(__file__).parent.parent / "artifacts" / "Hypertension_xgb_dynamic.joblib"

_HYPERTENSIONAGENT_CONFIG = AgentConfig(
    agent_name="Hypertension",
    description="Automated ensemble classifier generated from local data (synthetic_clinical_dataset.csv).",
    version="1.0.0-auto",
    artifact_path=_ARTIFACT_PATH,
    feature_names=['patient_id', 'age', 'bmi', 'systolic_bp', 'diastolic_bp', 'glucose', 'cholesterol', 'creatinine', 'diabetes', 'readmission_30d', 'mortality', 'sex_Male', 'sex_Other', 'diagnosis_Normal', 'diagnosis_Pneumonia', 'diagnosis_Sepsis'],
    target_classes=['0', '1'],
    trained_on_desc="Local Data via Automated Builder",
    n_splits=5
)

# --- 2. MCP Server Agent Class ---
class HypertensionAgent(DynamicMLAgent):
    """Dynamic binary classifier for Hypertension."""

    def __init__(self) -> None:
        super().__init__(_HYPERTENSIONAGENT_CONFIG)

    def sample_inputs(self) -> dict[str, Any]:
        """A representative sample case extracted during the build process."""
        return {
        "patient_id": 1001.0,
        "age": 63.0,
        "bmi": 24.1,
        "systolic_bp": 131.0,
        "diastolic_bp": 81.0,
        "glucose": 136.2,
        "cholesterol": 210.3,
        "creatinine": 1.04,
        "diabetes": 0.0,
        "readmission_30d": 0.0,
        "mortality": 0.0,
        "sex_Male": 1.0,
        "sex_Other": 0.0,
        "diagnosis_Normal": 0.0,
        "diagnosis_Pneumonia": 0.0,
        "diagnosis_Sepsis": 1.0
        }
